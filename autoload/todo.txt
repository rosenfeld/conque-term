
 * resize console window when buffer size changes
 X allow colors to be written to line more than once
 X slow subprocess loops to 1/sec when buffer is unfocused
 * investigate mintty attach problems (or don't)
 * use better way of determining if buffer length should be extended, e.g. cursor position
 * fix number syntax highlighting


